# Codex 项目入口

仓库级规则以 `AGENTS.md` 为准；当前事实以 `docs/PROJECT_STATE.md` 为准。

开始任务前完整阅读：

1. `AGENTS.md`
2. `docs/PROJECT_STATE.md`
3. `docs/collaboration/AI_HANDOFF_PLAYBOOK.md`
4. 任务指定的产品、体验、技术规格和已批准原型

不要沿用旧的注意力、桌宠、MBTI、冥想或 Linear 风格说明作为当前学生 MVP 基线。当前实现能力必须从指定 commit 的代码和测试确认，目标体验必须从学生 MVP 规格确认。

默认先做只读同步并报告 repository、branch、HEAD 和 working tree。当前工作区包含大量用户未提交内容；禁止笼统 stage、commit、reset、clean、stash 或切换分支。

未经用户明确授权，不 push、merge、发布、部署或修改域名。所有实现必须遵守“一名写入负责人，一名只读审查者”。

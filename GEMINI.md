# Gemini 项目入口

本文件不再保存独立产品定位或视觉 tokens，避免与其他 AI 漂移。

开始任务前按顺序阅读：

1. `AGENTS.md`
2. `docs/PROJECT_STATE.md`
3. `docs/collaboration/AI_HANDOFF_PLAYBOOK.md`
4. 任务指定的产品、体验、技术规格和已批准原型

研究或设计建议必须区分：

- `CURRENT`：指定 commit 已实现且可验证；
- `PARTIAL CURRENT`：已有底层能力，但目标交互尚未完成；
- `NEW CONCEPT`：尚未实现或尚未批准。

不得把旧 Linear 风格、聊天历史或外部原型当成当前视觉规范。只有用户批准并记录 manifest 的原型才能作为生产实现基线。

没有明确写入授权时默认只读；不修改仓库、不提交、不推送、不发布、不部署。
